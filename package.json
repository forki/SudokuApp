{
  "name": "meter-app",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "firstbuild": "npm install && fable",
    "prebuild": "npm install",
    "build": "fable --watch"
  },
  "dependencies": {
    "core-js": "^2.4.1",
    "fable-core": "^0.5.3",
    "react": "~15.2.0",
    "react-native": "^0.30.0"
  },
  "devDependencies": {
    "fable-compiler": "^0.5.3",
    "fable-import-react": "^0.5.0",
    "fable-import-react-native": "^0.6.12"
  }
}
